import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Welcome to FairPoint")
                .font(.largeTitle)
                .padding()
        }
    }
}
